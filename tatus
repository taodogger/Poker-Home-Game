[1mdiff --git a/js/app.js b/js/app.js[m
[1mindex cf0a75d..3dadf54 100644[m
[1m--- a/js/app.js[m
[1m+++ b/js/app.js[m
[36m@@ -488,6 +488,11 @@[m [mfunction updatePlayerList() {[m
     console.log(`[UI] Player list updated with ${PokerApp.state.players.length} players`);[m
 }[m
 [m
[32m+[m[32m// Add player function - handles both new players and rebuys[m
[32m+[m[32m// STARTING STACK LOGIC:[m
[32m+[m[32m// - For new players: initial_chips and current_chips both set to chips amount[m
[32m+[m[32m// - For rebuys: chips added to BOTH initial_chips (starting stack) and current_chips[m
[32m+[m[32m// - This ensures payout calculations work: profit/loss = current_chips - initial_chips[m
 function addPlayer(name, chips) {[m
     if (!name) {[m
         PokerApp.UI.showToast('Player name cannot be empty.', 'error');[m
[36m@@ -511,7 +516,8 @@[m [mfunction addPlayer(name, chips) {[m
         const originalCurrent = existingPlayer.current_chips;[m
         const originalInitial = existingPlayer.initial_chips;[m
         [m
[31m-        // Add chips to existing player (rebuy scenario)[m
[32m+[m[32m        // REBUY: Add chips to BOTH starting stack (initial_chips) and current chips[m
[32m+[m[32m        // This increases what they've paid in, which is used for payout calculations[m
         existingPlayer.current_chips += parseInt(chips);[m
         existingPlayer.initial_chips += parseInt(chips);[m
         existingPlayer.lastBuyIn = Date.now();[m
[36m@@ -2067,24 +2073,49 @@[m [mfunction setupGameStateListener(gameId) {[m
                             current_chips: parseInt(updatedPlayer.current_chips) || 0[m
                         };[m
                         [m
[31m-                        // Check if this is a host update that should take priority[m
[31m-                        const isHostUpdate = validatedUpdate.hostUpdated && validatedUpdate.lastHostUpdate;[m
[32m+[m[32m                        // HOST PRIORITY: Check if local player has recent host update[m
[32m+[m[32m                        const localHasRecentHostUpdate = existingPlayer.hostUpdated &&[m[41m [m
[32m+[m[32m                                                        existingPlayer.lastHostUpdate &&[m[41m [m
[32m+[m[32m                                                        (Date.now() - existingPlayer.lastHostUpdate < 10000); // 10 second protection window[m
[32m+[m[41m                        [m
[32m+[m[32m                        // Check if incoming update is a host update[m
[32m+[m[32m                        const isIncomingHostUpdate = validatedUpdate.hostUpdated && validatedUpdate.lastHostUpdate;[m
                         const isNewerThanLocal = !existingPlayer.lastHostUpdate || [m
                                               (validatedUpdate.lastHostUpdate > existingPlayer.lastHostUpdate);[m
                         [m
[31m-                        if (isHostUpdate) {[m
[31m-                            // IGNORE HOST UPDATES - they're handled locally only[m
[31m-                            console.log(`[FIREBASE_SYNC] Ignoring host update for ${existingPlayer.name} - handled locally`);[m
[32m+[m[32m                        // HOST AUTHORITY: Always prioritize local host updates over any Firebase changes[m
[32m+[m[32m                        if (localHasRecentHostUpdate) {[m
[32m+[m[32m                            console.log(`[FIREBASE_SYNC] BLOCKING update for ${existingPlayer.name} - local host update takes priority (${Date.now() - existingPlayer.lastHostUpdate}ms ago)`);[m
[32m+[m[32m                            return;[m
[32m+[m[32m                        }[m
[32m+[m[41m                        [m
[32m+[m[32m                        if (isIncomingHostUpdate) {[m
[32m+[m[32m                            // Only apply incoming host updates if they're newer than our local state[m
[32m+[m[32m                            if (isNewerThanLocal) {[m
[32m+[m[32m                                console.log(`[FIREBASE_SYNC] Applying newer host update for ${existingPlayer.name}`);[m
[32m+[m[32m                                existingPlayer.current_chips = validatedUpdate.current_chips;[m
[32m+[m[32m                                existingPlayer.lastHostUpdate = validatedUpdate.lastHostUpdate;[m
[32m+[m[32m                                existingPlayer.hostUpdated = true;[m
[32m+[m[32m                                updatePlayerList();[m
[32m+[m[32m                                saveState();[m
[32m+[m[32m                            } else {[m
[32m+[m[32m                                console.log(`[FIREBASE_SYNC] Ignoring older host update for ${existingPlayer.name}`);[m
[32m+[m[32m                            }[m
                             return;[m
                             [m
[31m-                        } else if (!isHostUpdate && !validatedUpdate.manualAdd && validatedUpdate.initial_chips > existingPlayer.initial_chips) {[m
[31m-                            // QR code rebuy only: Add chips to current total[m
[32m+[m[32m                        } else if (!isIncomingHostUpdate && !validatedUpdate.manualAdd && validatedUpdate.initial_chips > existingPlayer.initial_chips) {[m
[32m+[m[32m                            // QR CODE REBUY: Add chips to BOTH initial (starting stack) and current totals[m
                             const chipDiff = validatedUpdate.initial_chips - existingPlayer.initial_chips;[m
[31m-                            console.log(`[FIREBASE_SYNC] Applying QR code rebuy for ${existingPlayer.name}: +${chipDiff} chips`);[m
[32m+[m[32m                            console.log(`[FIREBASE_SYNC] Applying QR code rebuy for ${existingPlayer.name}: +${chipDiff} chips (starting stack: ${existingPlayer.initial_chips} -> ${validatedUpdate.initial_chips})`);[m
                             [m
[32m+[m[32m                            // Update starting stack (initial_chips) - this is what payouts calculate from[m
                             existingPlayer.initial_chips = validatedUpdate.initial_chips;[m
[32m+[m[32m                            // Add the difference to current chips[m
                             existingPlayer.current_chips += chipDiff;[m
                             [m
[32m+[m[32m                            // Save state to persist the starting stack update[m
[32m+[m[32m                            saveState();[m
[32m+[m[41m                            [m
                             // Update UI and trigger animation[m
                             updatePlayerList();[m
                             [m
[36m@@ -2794,11 +2825,14 @@[m [mfunction calculatePayouts() {[m
 [m
     console.log('[PAYOUT] Processing players:', players.length);[m
 [m
[31m-    // Calculate chip differences and initial cash values[m
[32m+[m[32m    // PAYOUT CALCULATION: Based on starting stack (initial_chips)[m
[32m+[m[32m    // - initial_chips = total amount player bought in for (starting stack + rebuys)[m
[32m+[m[32m    // - current_chips = current chip count (modified by host or game play)[m
[32m+[m[32m    // - chipDifference = profit/loss = current_chips - initial_chips[m
     const playerDiffs = players.map(player => {[m
[31m-        const initialChips = parseInt(player.initial_chips, 10) || 0;[m
[31m-        const currentChips = parseInt(player.current_chips, 10) || 0;[m
[31m-        const chipDifference = currentChips - initialChips;[m
[32m+[m[32m        const initialChips = parseInt(player.initial_chips, 10) || 0; // Starting stack (what they paid)[m
[32m+[m[32m        const currentChips = parseInt(player.current_chips, 10) || 0; // Current chips (after play)[m
[32m+[m[32m        const chipDifference = currentChips - initialChips; // Profit/loss in chips[m
         [m
         return {[m
             id: player.id,[m
[36m@@ -3502,6 +3536,10 @@[m [mfunction editPlayerChips(playerId) {[m
 window.editPlayerChips = editPlayerChips;[m
 [m
 // Function to update player chips directly from input field (HOST AUTHORITY)[m
[32m+[m[32m// HOST PRIORITY: This function has the HIGHEST priority for chip count changes[m
[32m+[m[32m// - Updates current_chips only (NOT initial_chips/starting stack)[m
[32m+[m[32m// - Payouts calculate profit/loss as: current_chips - initial_chips[m
[32m+[m[32m// - Host updates override any Firebase sync for 10 seconds[m
 function updatePlayerChips(playerId, newValue) {[m
     const player = PokerApp.state.players.find(p => p.id === playerId);[m
     const parsedNewValue = parseInt(newValue, 10);[m
[36m@@ -3517,9 +3555,10 @@[m [mfunction updatePlayerChips(playerId, newValue) {[m
         return;[m
     }[m
 [m
[31m-    console.log(`[HOST_UPDATE] Host updating ${player.name} chips to ${parsedNewValue} (was ${player.current_chips})`);[m
[32m+[m[32m    console.log(`[HOST_UPDATE] Host updating ${player.name} chips to ${parsedNewValue} (was ${player.current_chips}, starting stack: ${player.initial_chips})`);[m
 [m
[31m-    // ISOLATED LOCAL UPDATE - only affect this specific player[m
[32m+[m[32m    // HOST AUTHORITY: Update current chips only - starting stack (initial_chips) remains unchanged[m
[32m+[m[32m    // This ensures payout calculations work correctly: profit/loss = current_chips - initial_chips[m
     player.current_chips = parsedNewValue;[m
     player.lastHostUpdate = Date.now();[m
     player.hostUpdated = true;[m
