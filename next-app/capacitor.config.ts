import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taodogger.pokerhomegame',
  appName: 'Poker Home Game',
  webDir: 'out'
};

export default config;
